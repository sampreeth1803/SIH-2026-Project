# Server-side only. Never expose this key through VITE_* variables or commit .env.
TOMTOM_API_KEY=replace-with-your-tomtom-key

# Optional authorised private camera source. Keep credentials out of JSON and
# frontend variables, then reference this name with source_env in live_feeds.json.
# CAMERA_BLR_RTSP_URL=rtsp://camera-host.example/live
